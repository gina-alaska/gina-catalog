.container
  .panel.panel-default
    .panel-heading{ style: 'position: relative' }
      %ol.breadcrumb
        %li
          = link_to [:catalog, :contacts] do
            %i.fa.fa-group
            Map Layers
        %li.active
          Edit #{@map_layer.name}

    .panel-body
      = render 'form', map_layer: @map_layer 
