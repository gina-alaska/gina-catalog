%tr
  %td
    = entry.title
  %td
    = entry.status
  %td
    = entry.start_date
  %td
    = entry.end_date
  %td
    / placeholder for source organization
    = entry.primary_organizations.pluck(:acronym).join(', ')
  %td
    = can?(:manage, entry) ? "Yes" : "No"
  %td
    = entry.published? ? "Yes" : "No"
  %td
    / placeholder for downloads
    = entry.end_date
  %td.text-right
    - if can?(:manage, entry)
      .btn-group
        = link_to edit_catalog_entry_path(entry), class: 'btn btn-sm btn-primary', title: 'Edit' do
          %i.fa.fa-edit
        = link_to catalog_entry_path(entry), data: { method: :delete, confirm: "Are you sure?" }, class: 'btn btn-sm btn-danger', title: 'Delete' do
          %i.fa.fa-trash-o
