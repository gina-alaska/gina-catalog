%table.table.table-stripped.table-condensed
  %thead
    %tr
      %th Record Title
      %th Contact Name
      %th Contact Email
      %th Date

  %tbody
    - access_log.each do |ci|
      %tr
        %td
          = link_to edit_sds_admin_asset_path(ci.catalog) do
            = ci.catalog.title
        %td= ci.name
        %td= ci.email
        %td= ci.created_at.strftime('%F')