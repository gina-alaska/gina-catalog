%h4 
  Contact Information Form
- if current_catalog.require_contact_info?
  .alert.alert-info
    %p 
      %b The data steward has requested contact information for this download.  
- else
  .alert.alert-info
    %p 
      %b The data steward has requested contact information for this download.  
      
    %p You are not required to fill out this form and can proceed with the download by press the "Submit" button below.
  
= form_for current_contact_info, url: catalog_download_path(current_catalog, current_download), method: 'PUT' do |f|
  - if current_contact_info.errors.count > 0
    .alert.alert-error
      %h4 Error while trying to save contact information
      %ul
        - current_contact_info.errors.full_messages.each do |msg|
          %li= msg
  %fieldset
    .control-group
      %label.control-label{for: 'contact_info_name'} Name
      .controls
        = f.text_field :name, class: 'span12'
    .control-group
      %label.control-label{for: 'contact_info_email'} Email
      .controls
        = f.text_field :email, class: 'span12'
    .control-group
      %label.control-label{for: 'contact_info_usage_description'} Use Description
      .controls
        = f.text_area :usage_description, rows: 5, class: 'span12'
  = submit_tag 'Submit', class: 'btn btn-primary btn-large pull-right'