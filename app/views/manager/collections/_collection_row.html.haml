%tr
  %td
    = collection.name
  %td
    = collection.hidden.blank? ? "No" : "Yes"
  %td
    = collection.entry_collections_count
  %td.text-right
    .btn-group
      /TODO: Placeholder for show records assigned to this collection
      = link_to edit_manager_collection_path(collection), class: 'btn btn-sm btn-primary', title: 'Edit' do
        %i.fa.fa-edit
      = link_to manager_collection_path(collection), data: { method: :delete, confirm: "Are you sure you want to delete this collection?  It will be removed from all catalog records." }, class: 'btn btn-sm btn-danger', title: 'Delete' do
        %i.fa.fa-trash-o
