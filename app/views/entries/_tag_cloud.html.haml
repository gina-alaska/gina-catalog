- unless entry.tags.empty?
  %div{ style: 'border: 1px solid #dfdfdf; padding: 10px 10px 10px 10px' }
    - entry.tags.each do |tag|
      = link_to tag.name, manager_entries_path(search: { tags: [tag.name] }), class: 'btn btn-default btn-sm tag'