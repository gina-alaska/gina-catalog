/ %label Collections
%ul.nav.nav-tabs.nav-stacked.collection-list{ data: { intro: "This section lists the major collections by which the data records in the catalog are organized into. For each collection, the number of records in that collection is given. Click on a collection to view only search results for that collection.", step: 5, position: "top" } }
  - Collection.including_descendents(current_setup).order('name ASC').each do |coll|
    %li.clearfix{class: search_params[:collection_id].to_i == coll.id ? "active" : "", data: { collid: search_params[:collection_id] } }
      .badge.pull-right{ style: 'margin: 10px 5px 0 0;' }
        = @collection_facets[coll.id] || 0
        results
      = link_to "/search?search[collection_id]=#{coll.id}", data: {behaviour: "load_collection", target: "#search_collection_id", value: coll.id}, title: coll.description do
        = coll.name
        = " (#{coll.setup.title})" if coll.setup != current_setup
