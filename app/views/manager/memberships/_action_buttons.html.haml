.btn-group
  = link_to edit_manager_membership_path(item.id), class: 'btn', title: "Edit" do
    %i.icon-edit    
  = link_to manager_membership_path(item.id), method: :delete, class: 'btn btn-danger', confirm: "Are you sure?" do
    %i.icon-trash.icon-white
