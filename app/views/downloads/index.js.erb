<% if @catalog.sds? %>
  $('<%= escape_javascript(render("download_modal")) %>').modal().on('hidden', function() {
    $(this).remove();
  })
<% else %>
top.location = "<%= catalog_downloads_url(@catalog) %>"
<% end %>