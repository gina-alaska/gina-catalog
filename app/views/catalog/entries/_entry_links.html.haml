%dt Direct Record Link
%dd
  = link_to catalog_entry_url(entry) do
    = catalog_entry_url(entry)
- if entry.attachments.geojson.any? or entry.map_layers.any?
  %dt Direct Full Map Link
  %dd
    = link_to map_catalog_entry_path(entry) do
      = map_catalog_entry_url(entry)
