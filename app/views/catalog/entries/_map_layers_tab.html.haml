.row
  .col-sm-10
    %h4 Map Layers
  .col-sm-2
    = link_to new_catalog_entry_map_layer_path(@entry), class: 'btn btn-success pull-right', remote: true do
      %i.fa.fa-plus
      Add new Map Layer
.fields
  = render '/catalog/map_layers/list', map_layers: map_layers
