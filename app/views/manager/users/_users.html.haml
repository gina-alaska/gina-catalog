%table.table.table-striped.table-bordered.table-condensed
  %thead
    %tr
      %th Name
      %th Email
      %th Agency
      %th Roles
      %th.actions Actions
  %tbody
    - @users.each do |user|
      %tr
        %td= user.fullname
        %td= user.email
        %td= user.agency.try(:name_with_acronym)
        %td= user.roles.map(&:name).join(', ')
        %td.actions
          = link_to manager_user_path(user), class: 'btn', title: "Edit" do
            %i.icon-edit
