.navbar.navbar-static-top
  .navbar-inner
    .brand Setups
    .pull-right
      = link_to new_admin_setup_path, class: 'btn btn-success' do
        %i.icon-plus.icon-white
        New Setup

%ol.page-list
  - @setups.each do |setup|
    = render 'setup', setup: setup