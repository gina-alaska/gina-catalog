<% [:notice, :error, :success, :warning].each do |type| %>
  <% next if flash[type].blank? %>
  new FlashMessage('<%= type %>', '<%= flash[type] %>');
<% end %>
<% if @redirect_to %>
  top.location = "<%= @redirect_to %>";
<% end %>