.btn-group
  = link_to edit_manager_use_agreement_path(item.id), class: 'btn', title: "Edit" do
    %i.icon-edit    
  - if item.catalogs.count == 0
    = link_to manager_use_agreement_path(item.id), method: :delete, confirm: "Are you sure?", class: 'btn btn-danger', title: 'Remove Agreement', remote: true, 'data-loading-text'=> '<i class="icon-spinner icon-spin"></i>' do
      %i.icon-trash
