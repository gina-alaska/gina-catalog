.row
  .col-sm-4
    .panel.panel-default
      .panel-heading
        %h1.panel-title
          %i.fa.fa-cubes
          Catalog Records

      .panel-body
        = link_to new_manager_entry_path, class: 'btn btn-success btn-block' do
          New Catalog Record
    = render 'search'
  .col-sm-8
    %ul.media-list.entries
      = render @entries

      -# %table.table.table-striped.table-condensed
      -#   %thead
      -#     %tr
      -#       %th.col-md-2 Title
      -#       %th Status
      -#       %th Start Date
      -#       %th End Date
      -#       %th Primary Agency
      -#       %th Editable?
      -#       %th Published?
      -#       %th DLs
      -#       %th.text-right Actions
      -#
      -#   %tbody
      -#     = render(partial: "entry_row", collection: @entries, as: :entry) || "<tr><td colspan=\"9\">There are no records available</td></tr>".html_safe
