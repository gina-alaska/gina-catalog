.navbar.navbar-static-top
  .navbar-inner
    .brand Pages
    = link_to new_manager_page_content_path, class: 'btn btn-success pull-right' do
      %i.icon-plus.icon-white
      New Page
.page-list
  .row-fluid.header
    .span9.title Title
    .span3.actions Actions
  - current_setup.pages.roots.each do |page|
    - # shouldn't be necessary
    - next unless page.setups.include?(current_setup)
    = render '/manager/page_contents/page', page: page