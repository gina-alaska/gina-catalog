%tr
  %td= role.name
  %td= role.description
  %td.actions
    = link_to 'Edit', edit_manager_role_path(role), class: 'btn'
    = link_to 'Delete', manager_role_path(role), method: :delete, class: 'btn btn-danger', confirm: 'Are you sure?'